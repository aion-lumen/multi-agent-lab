# P0.4 Yahoo-Struktur-Move — Beleg-Artefakt (2026-07-12)

Referenzierbarer Beleg der real durchgeführten Yahoo-Umstrukturierung. Ersetzt die
frühere unbelegte Aussage „volume unverified". Zahlen sind read-only gegen Yahoo-IMAP
verifiziert (nach jedem Schritt Re-Inventur). Site-Änderung = Cowork-Kaskade; dies ist
die Fundstelle.

## Ausgangslage → Ziel

Altes `_AionLumen/*`-Klassifikations-Schema (verschachtelt) → flache Root-Domain-Struktur.
Council **aus** → generischer Move-Pfad (kein Konsens/uebernommen/Partner-Schritt).
Credentials bw-/timeout-frei über `password_cmd` (0600-Datei); Folio-Pfad ebenso.

## P0.4a — Ordner-Migration (Rename→Root)

| Aktion | Ordner | Mails |
|---|---|---:|
| RENAME | `_AionLumen/Immo` → `Immo` | 26 |
| RENAME | `_AionLumen/Job` → `Job` | 13 |
| RENAME | `_AionLumen/Shopping` → `Shopping` | 189 |
| MERGE | `_AionLumen/Korrespondenz` → `INBOX` | 8 |
| DELETE | `_AionLumen` (leerer Container) | — |

Origin-Snapshot (Message-IDs) vor jedem Schritt geschrieben (Umkehrbarkeit).

## P0.4c — INBOX → Root-Domain-Move (feedback.db-Klassifikation)

**Klassifikations-Qualität** (nach RFC-Header-Backfill + transaktionalen Prefixen):
personal_keep von **316 → 50** korrigiert; List-Unsubscribe primär, Sender-Prefix sekundär.

**Real verschoben** (Ordner-Delta, stale/`.example` via Filter ausgeschlossen):

| Bucket | real verschoben | pre-filter | Ordner-Endstand |
|---|---:|---:|---:|
| Immo | 170 | 211 | 196 |
| Job | 73 | 87 | 86 |
| Shopping | 58 | 60 | 247 |
| Finance | 7 | 8 | 7 |
| System | 1 | 1 | 1 |
| **Summe** | **309** | 367 | — |

**Im Eingang belassen (korrekt):**
- personal_keep: **50** (inkl. 4 echte Makler-Antworten — samuel.grauer/joerg.thalmann/
  adriana.russotto/gabriele.hofmann — read-only verifiziert unberührt)
- kontakt/unsorted (kein Ziel-Ordner): **70**

**Papierkorb:** 170 (Immo-INBOX-Duplikate aus einem unterbrochenen Lauf, per `move_to_trash`
recoverable 30 Tage — kein endgültiges Löschen; Immo-Originale bleiben im Ordner).

**INBOX:** 9798 → **9489** (−309, deckungsgleich mit den Domain-Moves).

## Verifikation (belegt)

- Nach jedem Move Re-Inventur; keine Duplikate INBOX↔Zielordner (COPY+EXPUNGE vollständig).
- 4 Makler-Antworten nach jedem Schritt read-only in INBOX bestätigt.
- Yahoo lehnt Batch-COPY ab → per-UID-Fallback (Grund für Hintergrund-Ausführung).

## Offene, separat getrackte Punkte

- `.example`-Seed-Daten (RFC-2606) in feedback.db: 35 Zeilen über ~24 Fake-Domains —
  kein echtes Mail, via Stale-Filter vom Move ausgeschlossen. Aufräum-Punkt (raus aus
  Produktiv-feedback.db), separat.
- `Archive/2026` (leer) unberührt; Papierkorb-Leeren (170) bleibt manueller/30-Tage-Schritt.
